Este es un texto que tiene control de versiones

linea 3

linea 5

linea 7

con "rm --cached" quitamos un archivo de memoria ram

con "git log" podemos ver nuestros commits realizados

con "git show" podemos ver las diferencias entre commits

si hacemos un commit sin "-m" y el mensaje git nos abrirá un editor de texto en terminal 

con "git checkout" podemos dar un vistaso a versiones anteriores del repositorio
para regresar al commit principal solo debemos poner "git checkout master"